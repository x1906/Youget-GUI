
// import os from 'os';

const settings = {
  execute: 'you-get',
  charset: 'UTF-8',
  // 用户下载目录
  // dir: `${os.homedir()}/Downloads`,
  dir: 'D:/Downloads',
  // 代理
  proxy: '',
};

export default settings;
